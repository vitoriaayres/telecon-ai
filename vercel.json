{
  "framework": "nextjs",
  "installCommand": "npm install --prefix telecon-ai && pip install -r requirements.txt",
  "buildCommand": "npm run build --prefix telecon-ai",
  "outputDirectory": "telecon-ai/.next"
}
